---
phase: 06-module-restructure-and-dom-cleanup
plan: 02
type: execute
wave: 2
depends_on:
  - 06-01
files_modified:
  - js/ui/battleModal.js
  - index.html
  - js/app.js
autonomous: true
requirements:
  - MODAL-01
  - MODAL-02
  - MODAL-03

must_haves:
  truths:
    - "A 'Start Battle' button is visible on the adventure sheet in the Actions section"
    - "The inline combat panel no longer appears on the adventure sheet"
    - "Tapping 'Start Battle' opens a modal overlay with the enemy setup form"
    - "The enemy setup form inside the modal has Name, Skill, Stamina inputs and a Start Combat button"
    - "The battle history section remains on the adventure sheet outside the modal"
  artifacts:
    - path: "js/ui/battleModal.js"
      provides: "Modal overlay lifecycle — create-on-open pattern"
      exports: ["openBattleModal", "closeBattleModal"]
    - path: "index.html"
      provides: "Adventure sheet with Start Battle button, no inline combat section"
      contains: "start-battle-btn"
    - path: "js/app.js"
      provides: "Wiring: Start Battle click -> openBattleModal()"
      contains: "openBattleModal"
  key_links:
    - from: "js/app.js"
      to: "js/ui/battleModal.js"
      via: "import { openBattleModal } from './ui/battleModal.js'"
      pattern: "import.*openBattleModal.*battleModal"
    - from: "js/ui/battleModal.js"
      to: "js/ui/battle.js"
      via: "import { renderBattle } from './battle.js'"
      pattern: "import.*renderBattle.*battle"
    - from: "js/app.js:#start-battle-btn click"
      to: "openBattleModal(getState, callbacks)"
      via: "addEventListener click handler"
      pattern: "start-battle-btn.*addEventListener"
---

<objective>
Create battleModal.js (modal overlay skeleton), remove the inline combat section from index.html, add the "Start Battle" trigger button, and rewire app.js to use the modal instead of the inline panel. This is the user-facing change that moves combat from the adventure sheet into a modal overlay.

Purpose: Implement MODAL-01 (Start Battle button), MODAL-02 (no inline combat panel), and MODAL-03 (enemy setup form in modal).
Output: New js/ui/battleModal.js file, updated index.html, updated js/app.js.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06-module-restructure-and-dom-cleanup/06-CONTEXT.md
@.planning/phases/06-module-restructure-and-dom-cleanup/06-RESEARCH.md
@.planning/phases/06-module-restructure-and-dom-cleanup/06-UI-SPEC.md
@.planning/phases/06-module-restructure-and-dom-cleanup/06-01-SUMMARY.md

<interfaces>
<!-- After Plan 01, battle.js exports these signatures: -->
From js/ui/battle.js (post-refactor):
```javascript
export function renderBattle(container, getState, callbacks, historyContainer = null) { ... }
export async function loadCombatHistory(bookNumber, historyContainer) { ... }
```

From js/ui/charCreate.js (reference pattern for modal overlay):
```javascript
// Create overlay
const overlay = document.createElement('div');
overlay.className = 'modal-overlay active';
overlay.innerHTML = `...`;
document.body.appendChild(overlay);

// Scope all queries to overlay
const someEl = overlay.querySelector('#some-id');

// Destroy
function cleanup() { overlay.remove(); }
```

From js/app.js (current imports to change):
```javascript
import { renderBattle, loadCombatHistory } from './ui/battle.js';
```

From css/style.css (existing modal styles — no CSS changes needed):
```css
.modal-overlay { position: fixed; inset: 0; background: rgba(0,0,0,.5); display: none; ... }
.modal-overlay.active { display: flex; }
.modal { background: var(--paper-bg); border-radius: 12px; padding: 20px; ... }
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create battleModal.js with modal overlay skeleton</name>
  <files>js/ui/battleModal.js</files>
  <read_first>
    - js/ui/charCreate.js (reference implementation — follow create-on-open/destroy-on-close pattern exactly)
    - js/ui/battle.js (post Plan 01 — verify renderBattle signature is (container, getState, callbacks, historyContainer))
    - .planning/phases/06-module-restructure-and-dom-cleanup/06-RESEARCH.md (Pattern 2 section — exact modal HTML template)
    - .planning/phases/06-module-restructure-and-dom-cleanup/06-UI-SPEC.md (Component Inventory section 4 — modal inner HTML structure)
    - css/style.css (lines 276-312 — confirm .modal-overlay, .modal, .modal-overlay.active rules exist)
  </read_first>
  <action>
Create new file `js/ui/battleModal.js` following the charCreate.js create-on-open / destroy-on-close pattern per D-06, D-07, D-08, D-09.

**File structure:**

```javascript
/**
 * Battle modal overlay — creates/destroys modal on open/close.
 * Follows charCreate.js pattern: create-on-open, destroy-on-close.
 * Does NOT import app.js (D-17 / D-09 pattern).
 */

import { renderBattle } from './battle.js';
```

**openBattleModal(getState, callbacks) export:**

1. Create overlay element: `document.createElement('div')`, set `className = 'modal-overlay active'`
2. Set `overlay.innerHTML` to the full combat modal HTML structure per D-07. The exact HTML (from 06-RESEARCH.md Pattern 2):

```html
<div class="modal battle-modal">
    <h2 class="modal-title">Combat</h2>
    <div class="combat-setup" id="combat-setup">
        <div class="combat-input-row">
            <label class="combat-label" for="enemy-name">Enemy</label>
            <input type="text" class="combat-input" id="enemy-name" placeholder="Name">
        </div>
        <div class="combat-input-row">
            <label class="combat-label" for="enemy-skill">Skill</label>
            <input type="number" class="combat-input combat-input--stat"
                   id="enemy-skill" min="1" max="20" value="8">
        </div>
        <div class="combat-input-row">
            <label class="combat-label" for="enemy-stamina">Stamina</label>
            <input type="number" class="combat-input combat-input--stat"
                   id="enemy-stamina" min="1" max="30" value="8">
        </div>
        <button class="mechanic-btn mechanic-btn--primary" id="start-combat">
            Start Combat
        </button>
    </div>
    <div class="combat-active" id="combat-active" hidden>
        <p class="combat-status" id="combat-status"></p>
        <div class="stamina-bar-group" id="combat-stamina-bars">
            <div class="stamina-bar-row">
                <span class="stamina-bar-label">You</span>
                <div class="stamina-bar" id="player-stamina-bar"
                     role="progressbar" aria-valuemin="0" aria-valuemax="20" aria-valuenow="20">
                    <div class="stamina-bar__fill stamina-bar__fill--player"
                         id="player-stamina-fill"></div>
                </div>
                <span class="stamina-bar-value" id="player-stamina-value"></span>
            </div>
            <div class="stamina-bar-row">
                <span class="stamina-bar-label" id="enemy-stamina-label">Enemy</span>
                <div class="stamina-bar" id="enemy-stamina-bar"
                     role="progressbar" aria-valuemin="0" aria-valuemax="8" aria-valuenow="8">
                    <div class="stamina-bar__fill stamina-bar__fill--enemy"
                         id="enemy-stamina-fill"></div>
                </div>
                <span class="stamina-bar-value" id="enemy-stamina-value"></span>
            </div>
        </div>
        <div id="combat-round-result"></div>
        <p class="mechanic-result" id="combat-result" aria-live="polite"></p>
        <div class="combat-actions">
            <button class="mechanic-btn mechanic-btn--primary" id="roll-round">Roll Round</button>
            <button class="mechanic-btn" id="flee-combat">Flee</button>
        </div>
        <div id="combat-summary" hidden></div>
    </div>
</div>
```

3. Append overlay to document.body: `document.body.appendChild(overlay)`
4. Get the inner modal div: `const modalEl = overlay.querySelector('.battle-modal')`
5. Get the history container from the main page (it stays in index.html): `const historyContainer = document.getElementById('combat-history')`
6. Call renderBattle: `renderBattle(modalEl, getState, callbacks, historyContainer)`

**closeBattleModal() stub export (per Claude's discretion — provides symbol for Phase 7):**

```javascript
export function closeBattleModal() {
    // Phase 7 implements full teardown (overlay.remove(), scroll unlock, etc.)
}
```

**Module boundary check (D-09):** The file imports ONLY from `'./battle.js'`. It does NOT import app.js. It receives getState and callbacks as arguments.
  </action>
  <verify>
    <automated>cd /home/claude/ffconsole && test -f js/ui/battleModal.js && grep -q "export function openBattleModal" js/ui/battleModal.js && grep -q "import.*renderBattle.*battle" js/ui/battleModal.js && ! grep -q "import.*app" js/ui/battleModal.js && echo "PASS" || echo "FAIL"</automated>
  </verify>
  <acceptance_criteria>
    - js/ui/battleModal.js exists
    - File contains `export function openBattleModal(getState, callbacks)`
    - File contains `export function closeBattleModal()`
    - File contains `import { renderBattle } from './battle.js'`
    - File does NOT contain any import from `app.js` (D-09 check)
    - File contains `modal-overlay active` (overlay className)
    - File contains `document.body.appendChild(overlay)` (create-on-open pattern)
    - File contains `overlay.querySelector('.battle-modal')` (passes inner modal div, not overlay)
    - File contains `renderBattle(modalEl, getState, callbacks, historyContainer)` (4-arg call)
    - Modal HTML includes id="combat-setup", id="combat-active" with hidden attribute, id="combat-summary" with hidden attribute
    - Modal HTML includes id="enemy-name", id="enemy-skill", id="enemy-stamina" inputs
    - Modal HTML includes id="start-combat" button with class "mechanic-btn mechanic-btn--primary"
    - Modal title is "Combat" in an h2.modal-title
  </acceptance_criteria>
  <done>
    js/ui/battleModal.js exists with openBattleModal() and closeBattleModal() exports. It follows the charCreate.js create-on-open pattern. The modal HTML includes the full combat structure: visible setup form + hidden active section + hidden summary section. renderBattle() is called with the inner .battle-modal div as container.
  </done>
</task>

<task type="auto">
  <name>Task 2: Update index.html and app.js — remove inline combat, add Start Battle trigger, rewire imports</name>
  <files>index.html, js/app.js</files>
  <read_first>
    - index.html (full file — understand current structure before removing combat-section)
    - js/app.js (full file — understand current battle wiring at lines 71-90 and import at line 12)
    - js/ui/battleModal.js (just created in Task 1 — verify openBattleModal export exists)
    - .planning/phases/06-module-restructure-and-dom-cleanup/06-CONTEXT.md (D-01, D-02, D-03 — button placement and section rename)
    - .planning/phases/06-module-restructure-and-dom-cleanup/06-RESEARCH.md (Pattern 4 — exact HTML changes)
  </read_first>
  <action>
**index.html changes (per D-01, D-02, D-03):**

1. **Rename Tests section to Actions (D-01).** Change lines 64-70 from:
```html
<section class="mechanics-section tests-section">
    <h2 class="mechanics-title">Tests</h2>
    <div class="test-buttons">
        <button class="mechanic-btn" id="test-luck">Test Luck</button>
    </div>
    <div class="luck-result" id="luck-result" aria-live="polite"></div>
</section>
```
to:
```html
<section class="mechanics-section actions-section">
    <h2 class="mechanics-title">Actions</h2>
    <div class="test-buttons">
        <button class="mechanic-btn" id="test-luck">Test Luck</button>
        <button class="mechanic-btn mechanic-btn--primary" id="start-battle-btn">Start Battle</button>
    </div>
    <div class="luck-result" id="luck-result" aria-live="polite"></div>
</section>
```

Key changes: class `tests-section` -> `actions-section`, heading text `Tests` -> `Actions`, new `#start-battle-btn` button added after `#test-luck` with classes `mechanic-btn mechanic-btn--primary` (per D-03).

2. **Remove the entire combat-section (D-02).** Delete lines 76-130 (the `<section class="mechanics-section combat-section">` and everything inside it, up to and including its closing `</section>` tag). This removes all inline combat HTML: combat-setup, combat-active, stamina bars, round result, combat actions, combat summary.

3. **Keep combat-history-section unchanged.** The `<section class="mechanics-section combat-history-section" id="combat-history-section">` (lines 132-135) stays exactly as-is.

**js/app.js changes (per D-08, D-09):**

1. **Change the battle import line.** Replace:
```javascript
import { renderBattle, loadCombatHistory } from './ui/battle.js';
```
with:
```javascript
import { loadCombatHistory } from './ui/battle.js';
import { openBattleModal } from './ui/battleModal.js';
```

2. **Replace the inline battle panel initialisation block.** Remove the block at lines 72-84:
```javascript
const combatSection = document.querySelector('.combat-section');
if (combatSection) {
    renderBattle(combatSection, () => ({ state, combatState, currentBook }), {
        ...callbacks...
    });
}
```

Replace with the Start Battle button wiring:
```javascript
const startBattleBtn = document.getElementById('start-battle-btn');
if (startBattleBtn) {
    startBattleBtn.addEventListener('click', () => {
        openBattleModal(
            () => ({ state, combatState, currentBook }),
            {
                onStart: startCombat,
                onRollRound: rollCombatRound,
                onFlee: endCombat,
                onEnd: endCombat,
                onStatSync: syncStateFromServer,
                onCombatEnd: () => { combatState.active = false; },
                onTestLuck: (bookNumber, luckCurrent, round, context, damageBefore) =>
                    testCombatLuck(bookNumber, luckCurrent, round, context, damageBefore),
            }
        );
    });
}
```

3. **Keep the history loading block unchanged** (lines 87-90):
```javascript
const historyContainer = document.getElementById('combat-history');
if (currentBook && historyContainer) {
    loadCombatHistory(currentBook, historyContainer);
}
```

This stays because the history section remains in index.html.
  </action>
  <verify>
    <automated>cd /home/claude/ffconsole && grep -q 'start-battle-btn' index.html && ! grep -q 'combat-section' index.html && grep -q 'combat-history-section' index.html && grep -q 'openBattleModal' js/app.js && ! grep -q 'renderBattle' js/app.js && grep -q "import.*loadCombatHistory.*battle" js/app.js && echo "PASS" || echo "FAIL"</automated>
  </verify>
  <acceptance_criteria>
    - index.html contains `id="start-battle-btn"` with classes `mechanic-btn mechanic-btn--primary`
    - index.html contains `<h2 class="mechanics-title">Actions</h2>` (not "Tests")
    - index.html contains `class="mechanics-section actions-section"` (not "tests-section")
    - index.html does NOT contain `class="mechanics-section combat-section"` (section removed)
    - index.html does NOT contain `id="combat-setup"` (moved to modal-generated DOM)
    - index.html does NOT contain `id="combat-active"` (moved to modal-generated DOM)
    - index.html DOES contain `id="combat-history-section"` (history stays)
    - index.html DOES contain `id="combat-history"` (history container stays)
    - js/app.js contains `import { openBattleModal } from './ui/battleModal.js'`
    - js/app.js contains `import { loadCombatHistory } from './ui/battle.js'`
    - js/app.js does NOT contain `import { renderBattle` (no longer imported directly)
    - js/app.js does NOT contain `document.querySelector('.combat-section')` (removed)
    - js/app.js contains `getElementById('start-battle-btn')` and `openBattleModal(` in its init function
    - js/app.js still contains the `loadCombatHistory(currentBook, historyContainer)` call
  </acceptance_criteria>
  <done>
    index.html has a "Start Battle" button in the Actions section, no inline combat panel, and the battle history section is unchanged. app.js imports openBattleModal from battleModal.js, wires the Start Battle click handler, and no longer calls renderBattle directly. loadCombatHistory is still imported from battle.js for the history section.
  </done>
</task>

</tasks>

<verification>
1. `grep -c 'combat-section' index.html` returns 0 (inline combat removed)
2. `grep 'start-battle-btn' index.html` returns a match (trigger button exists)
3. `grep 'Actions' index.html` returns a match (section renamed)
4. `grep 'openBattleModal' js/app.js` returns a match (wired)
5. `grep 'renderBattle' js/app.js` returns NO match (no longer called directly)
6. `grep 'combat-history-section' index.html` returns a match (history stays)
7. App loads without JS errors: start server with `uvicorn backend.main:app --host 0.0.0.0 --port 3000`, load page, check console
</verification>

<success_criteria>
- "Start Battle" button visible on adventure sheet in the "Actions" section alongside "Test Luck"
- No inline combat panel on the adventure sheet (combat-section removed from index.html)
- Tapping "Start Battle" opens a modal overlay showing the enemy setup form (Name, Skill, Stamina inputs + Start Combat button)
- Battle history section remains on the adventure sheet unchanged
- No JavaScript errors on page load or when opening the modal
- battleModal.js follows D-17 pattern (no app.js imports)
</success_criteria>

<output>
After completion, create `.planning/phases/06-module-restructure-and-dom-cleanup/06-02-SUMMARY.md`
</output>
