---
phase: 03-battle-system
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - backend/routers/actions.py
autonomous: true
requirements:
  - BATTLE-05
must_haves:
  truths:
    - "When a combat ends with winner='fled', the player loses 2 Stamina server-side"
  artifacts:
    - path: "backend/routers/actions.py"
      provides: "Flee Stamina penalty applied atomically"
      contains: "combat_end"
  key_links:
    - from: "backend/routers/actions.py"
      to: "Session.stamina_current"
      via: "combat_end handler with winner=='fled'"
      pattern: "combat_end.*fled.*stamina_current"
---

<objective>
Fix the flee Stamina penalty gap in the backend actions router so that fleeing combat deducts 2 Stamina server-side.

Purpose: Without this fix, fleeing combat appears to work but the Stamina penalty is not persisted. On reload or device switch, the player's Stamina would be wrong. This is the only backend change needed for Phase 3.
Output: Updated `backend/routers/actions.py` with flee handling.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@backend/routers/actions.py
@backend/schemas.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add flee Stamina deduction to combat_end handler in actions.py</name>
  <files>backend/routers/actions.py</files>
  <action>
In `backend/routers/actions.py`, inside the `post_action` function (line 42), add a new `elif` branch after the `combat_round` handler (after line 53). The new branch handles `combat_end` when the player flees:

```python
elif body.action_type == "combat_end":
    winner = body.details.get("winner")
    if winner == "fled":
        session.stamina_current = max(0, session.stamina_current - 2)
        session.updated_at = _now()
```

This goes between line 53 (end of `combat_round` block) and line 55 (the `log = ActionLog(...)` line).

The pattern matches the existing `combat_round` / `enemy_hit` handler: check action_type, read details, apply stat mutation, update timestamp. The `max(0, ...)` guard prevents negative Stamina (same pattern as line 52).

Do NOT add a new `combat_flee` action type -- reuse the existing `combat_end` action type that app.js already POSTs with `winner='fled'` (see app.js line 492: `endCombat(currentBook, 'fled', ...)`).
  </action>
  <verify>
    <automated>cd /home/claude/ffconsole && python -c "
import importlib, sys
sys.path.insert(0, '.')
source = open('backend/routers/actions.py').read()
assert 'combat_end' in source, 'combat_end not found'
assert 'fled' in source, 'fled not found'
assert 'stamina_current' in source.split('combat_end')[1], 'stamina deduction not found after combat_end'
print('PASS: flee handler present in actions.py')
"</automated>
  </verify>
  <done>The `post_action` endpoint deducts 2 Stamina when `action_type == 'combat_end'` and `details.winner == 'fled'`. The returned SessionResponse reflects the updated Stamina.</done>
</task>

</tasks>

<verification>
Start the backend with `uvicorn backend.main:app --port 3000` and confirm:
1. POST `/api/sessions/{book}/actions` with `{"action_type": "combat_end", "details": {"winner": "fled", "final_player_stamina": 10, "final_enemy_stamina": 5, "rounds": 3, "enemy_name": "Goblin"}}` returns a session with `stamina.current` reduced by 2.
2. POST with `{"action_type": "combat_end", "details": {"winner": "player", ...}}` does NOT deduct Stamina.
</verification>

<success_criteria>
- Flee combat deducts exactly 2 from `stamina_current` server-side
- Non-flee combat_end (winner = "player" or "enemy") does not deduct Stamina
- Stamina cannot go below 0 (max(0, ...) guard)
</success_criteria>

<output>
After completion, create `.planning/phases/03-battle-system/03-01-SUMMARY.md`
</output>
