---
phase: 03-battle-system
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - index.html
autonomous: true
requirements:
  - BATTLE-03
  - BATTLE-04
  - BATTLE-06
  - BATTLE-07
  - BATTLE-08
must_haves:
  truths:
    - "Combat active section contains Stamina bar elements for both player and enemy"
    - "A container exists for the round result card"
    - "A hidden summary panel exists for post-battle display"
    - "A combat history section exists below the combat panel"
  artifacts:
    - path: "index.html"
      provides: "Battle UI HTML scaffolding"
      contains: "combat-history"
  key_links:
    - from: "index.html"
      to: "js/ui/battle.js"
      via: "DOM element IDs that battle.js will query"
      pattern: "combat-stamina-bars|combat-round-result|combat-summary|combat-history"
---

<objective>
Add HTML scaffolding to index.html for the battle UI: Stamina bar containers, round result area, post-battle summary panel, and combat history section.

Purpose: battle.js (plan 03-04) needs these DOM elements to render into. Adding them as static HTML means the module can querySelector them immediately.
Output: Updated `index.html` with all battle UI containers.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@index.html
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add battle UI elements to the combat section in index.html</name>
  <files>index.html</files>
  <action>
Modify the `#combat-active` div in `index.html` (lines 92-100). Replace the current contents of `#combat-active` with a richer structure. The existing elements (`combat-status`, `combat-enemy-stamina`, `combat-result`, `roll-round`, `flee-combat`) stay but are reorganized with new elements added.

Replace lines 92-100 with:

```html
            <div class="combat-active" id="combat-active" hidden>
                <p class="combat-status" id="combat-status"></p>

                <!-- Stamina bars (BATTLE-04) -->
                <div class="stamina-bar-group" id="combat-stamina-bars">
                    <div class="stamina-bar-row">
                        <span class="stamina-bar-label">You</span>
                        <div class="stamina-bar" id="player-stamina-bar" role="progressbar" aria-valuemin="0" aria-valuemax="20" aria-valuenow="20">
                            <div class="stamina-bar__fill stamina-bar__fill--player" id="player-stamina-fill"></div>
                        </div>
                        <span class="stamina-bar-value" id="player-stamina-value"></span>
                    </div>
                    <div class="stamina-bar-row">
                        <span class="stamina-bar-label" id="enemy-stamina-label">Enemy</span>
                        <div class="stamina-bar" id="enemy-stamina-bar" role="progressbar" aria-valuemin="0" aria-valuemax="8" aria-valuenow="8">
                            <div class="stamina-bar__fill stamina-bar__fill--enemy" id="enemy-stamina-fill"></div>
                        </div>
                        <span class="stamina-bar-value" id="enemy-stamina-value"></span>
                    </div>
                </div>

                <!-- Round result card (BATTLE-03) -->
                <div id="combat-round-result"></div>

                <!-- Legacy result for screen readers -->
                <p class="mechanic-result" id="combat-result" aria-live="polite"></p>

                <div class="combat-actions">
                    <button class="mechanic-btn mechanic-btn--primary" id="roll-round">Roll Round</button>
                    <button class="mechanic-btn" id="flee-combat">Flee</button>
                </div>

                <!-- Post-battle summary (BATTLE-06), hidden until combat ends -->
                <div id="combat-summary" hidden></div>
            </div>
```

Then, AFTER the closing `</section>` of the combat section (after line 101), add a new combat history section:

```html
        <section class="mechanics-section combat-history-section" id="combat-history-section">
            <h2 class="mechanics-title">Battle History</h2>
            <div id="combat-history"></div>
        </section>
```

This goes between the combat section closing tag and the actions section (`<section class="actions-section">`).

**Key element IDs that battle.js will use:**
- `#combat-stamina-bars` — Stamina bar container
- `#player-stamina-bar`, `#player-stamina-fill`, `#player-stamina-value` — player bar
- `#enemy-stamina-bar`, `#enemy-stamina-fill`, `#enemy-stamina-value`, `#enemy-stamina-label` — enemy bar
- `#combat-round-result` — round card container (battle.js injects HTML here)
- `#combat-summary` — summary container (battle.js injects HTML here, unhides after combat end)
- `#combat-history` — history log container (battle.js loads and renders past battles here)

Remove the old `<p class="combat-enemy-stamina" id="combat-enemy-stamina"></p>` line (line 94 in original) — this text-only enemy stamina display is replaced by the Stamina bar.
  </action>
  <verify>
    <automated>cd /home/claude/ffconsole && python -c "
html = open('index.html').read()
ids = ['combat-stamina-bars', 'player-stamina-bar', 'player-stamina-fill', 'enemy-stamina-bar', 'enemy-stamina-fill', 'combat-round-result', 'combat-summary', 'combat-history']
missing = [i for i in ids if f'id=\"{i}\"' not in html]
assert not missing, f'Missing element IDs: {missing}'
# Verify old enemy stamina text element is gone
assert 'combat-enemy-stamina' not in html or 'enemy-stamina-label' in html, 'Old enemy stamina element still present'
print('PASS: All battle HTML elements present')
"</automated>
  </verify>
  <done>index.html contains all container elements for Stamina bars, round result card, post-battle summary, and combat history section. All elements have unique IDs matching the battle.js contract.</done>
</task>

</tasks>

<verification>
Open `index.html` and confirm:
1. `#combat-active` contains Stamina bar markup with proper ARIA roles
2. `#combat-round-result` div exists (empty, to be filled by JS)
3. `#combat-summary` div exists with `hidden` attribute
4. `#combat-history` section exists after the combat section
5. Old `#combat-enemy-stamina` paragraph is removed
6. Existing `#roll-round` and `#flee-combat` buttons are preserved
</verification>

<success_criteria>
- All 8 new element IDs are present in index.html
- Stamina bars have proper ARIA progressbar attributes
- combat-summary starts hidden
- Battle History section exists with `#combat-history` container
- No duplicate element IDs
</success_criteria>

<output>
After completion, create `.planning/phases/03-battle-system/03-03-SUMMARY.md`
</output>
