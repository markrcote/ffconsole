---
phase: 03-battle-system
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - css/style.css
autonomous: true
requirements:
  - BATTLE-03
  - BATTLE-04
  - BATTLE-06
  - BATTLE-08
must_haves:
  truths:
    - "Stamina bars render as horizontal progress bars with fill elements"
    - "Round result cards have a distinct visual container"
    - "Post-battle summary has its own styled panel"
    - "Combat history entries are styled and readable"
  artifacts:
    - path: "css/style.css"
      provides: "All battle UI CSS classes"
      contains: "stamina-bar"
  key_links:
    - from: "css/style.css"
      to: "index.html"
      via: "CSS classes referenced by battle HTML elements"
      pattern: "\\.stamina-bar|\\.combat-round-card|\\.combat-summary|\\.combat-log"
---

<objective>
Add all CSS styles needed for the battle UI: Stamina bars, round result cards, post-battle summary panel, and combat history log.

Purpose: All new HTML elements (plans 03-03 and 03-04) need styling. Doing CSS first means the HTML scaffolding and JS module can be verified visually as soon as they land.
Output: Updated `css/style.css` with battle-specific classes.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@css/style.css
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add Stamina bar and round card CSS</name>
  <files>css/style.css</files>
  <action>
Add the following CSS rules to `css/style.css` AFTER the existing `.combat-actions` block (after line 547) and BEFORE the `/* Responsive */` comment (line 549). Insert them in the `/* Combat */` section.

**Stamina bars** (BATTLE-04):
```css
.stamina-bar-group {
    display: flex;
    flex-direction: column;
    gap: 8px;
    margin-bottom: 12px;
}

.stamina-bar-row {
    display: flex;
    align-items: center;
    gap: 10px;
}

.stamina-bar-label {
    font-family: 'MedievalSharp', cursive;
    font-size: 0.8rem;
    text-transform: uppercase;
    letter-spacing: 1px;
    color: var(--ink-color);
    min-width: 70px;
}

.stamina-bar-value {
    font-family: 'Caveat', cursive;
    font-size: 1rem;
    color: var(--ink-light);
    min-width: 40px;
    text-align: right;
}

.stamina-bar {
    flex: 1;
    height: 14px;
    background: var(--parchment-stain);
    border: 1px solid var(--ink-light);
    border-radius: 7px;
    overflow: hidden;
}

.stamina-bar__fill {
    height: 100%;
    border-radius: 7px;
    transition: width 0.3s ease;
}

.stamina-bar__fill--player {
    background: var(--accent-green, #5a8a3c);
}

.stamina-bar__fill--enemy {
    background: var(--accent-red);
}

.stamina-bar__fill--critical {
    background: var(--accent-red);
    animation: pulse-bar 1s infinite;
}

@keyframes pulse-bar {
    0%, 100% { opacity: 1; }
    50% { opacity: 0.6; }
}
```

**Round result card** (BATTLE-03):
```css
.combat-round-card {
    background: var(--paper-bg);
    border: 2px solid var(--ink-light);
    border-radius: 6px;
    padding: 10px 12px;
    margin: 10px 0;
}

.combat-round-card__header {
    font-family: 'MedievalSharp', cursive;
    font-size: 0.85rem;
    text-transform: uppercase;
    letter-spacing: 1px;
    color: var(--ink-light);
    margin-bottom: 8px;
}

.combat-round-card__row {
    display: flex;
    align-items: center;
    gap: 6px;
    margin-bottom: 4px;
    font-family: 'Caveat', cursive;
    font-size: 1.1rem;
    color: var(--ink-color);
}

.combat-round-card__row-label {
    min-width: 50px;
    font-size: 0.9rem;
}

.combat-round-card__as {
    font-weight: bold;
    margin-left: auto;
    font-size: 1.2rem;
}

.combat-round-card__outcome {
    font-family: 'MedievalSharp', cursive;
    font-size: 1rem;
    margin-top: 8px;
    padding-top: 6px;
    border-top: 1px solid var(--parchment-stain);
}

.combat-round-card__outcome--player-hit {
    color: var(--accent-green, #5a8a3c);
}

.combat-round-card__outcome--enemy-hit {
    color: var(--accent-red);
}

.combat-round-card__outcome--tie {
    color: var(--ink-light);
}
```

**Post-battle summary** (BATTLE-06):
```css
.combat-summary {
    background: var(--paper-bg);
    border: 2px solid var(--ink-color);
    border-radius: 8px;
    padding: 14px 16px;
    margin: 12px 0;
    text-align: center;
}

.combat-summary__title {
    font-family: 'MedievalSharp', cursive;
    font-size: 1.2rem;
    text-transform: uppercase;
    letter-spacing: 2px;
    margin-bottom: 8px;
}

.combat-summary__title--victory {
    color: var(--accent-green, #5a8a3c);
}

.combat-summary__title--defeat {
    color: var(--accent-red);
}

.combat-summary__title--fled {
    color: var(--ink-light);
}

.combat-summary__stats {
    display: flex;
    justify-content: center;
    gap: 20px;
    font-family: 'Caveat', cursive;
    font-size: 1.1rem;
    color: var(--ink-color);
    margin-bottom: 8px;
}

.combat-summary__stat-label {
    font-size: 0.85rem;
    color: var(--ink-light);
    display: block;
}
```

**Combat history log** (BATTLE-08):
```css
.combat-log {
    margin-top: 16px;
}

.combat-log__title {
    font-family: 'MedievalSharp', cursive;
    font-size: 0.9rem;
    text-transform: uppercase;
    letter-spacing: 1px;
    color: var(--ink-light);
    margin-bottom: 8px;
    cursor: pointer;
}

.combat-log__title::before {
    content: '\25B6';
    display: inline-block;
    margin-right: 6px;
    font-size: 0.7rem;
    transition: transform 0.2s;
}

.combat-log__title.expanded::before {
    transform: rotate(90deg);
}

.combat-log__battles {
    display: flex;
    flex-direction: column;
    gap: 10px;
}

.combat-log__battle {
    border: 1px solid var(--parchment-stain);
    border-radius: 6px;
    padding: 8px 10px;
}

.combat-log__battle-header {
    font-family: 'MedievalSharp', cursive;
    font-size: 0.85rem;
    color: var(--ink-color);
    margin-bottom: 4px;
}

.combat-log__entry {
    font-family: 'Caveat', cursive;
    font-size: 0.95rem;
    color: var(--ink-light);
    padding: 2px 0;
}
```

Also add to the responsive section (inside the `@media (max-width: 500px)` block, after existing combat-actions rule on line ~607):
```css
    .combat-summary__stats {
        flex-direction: column;
        gap: 8px;
    }
```

Ensure `--accent-green` is defined. Check if `:root` already has it; if not, add `--accent-green: #5a8a3c;` to the `:root` block at the top of the file.
  </action>
  <verify>
    <automated>cd /home/claude/ffconsole && python -c "
css = open('css/style.css').read()
classes = ['.stamina-bar ', '.stamina-bar__fill', '.combat-round-card', '.combat-summary', '.combat-log', '.combat-log__entry', '.combat-log__battle']
missing = [c for c in classes if c not in css]
assert not missing, f'Missing CSS classes: {missing}'
print('PASS: All battle CSS classes present')
"</automated>
  </verify>
  <done>All CSS classes for Stamina bars, round cards, summary panel, and history log are defined. Responsive rules included for mobile.</done>
</task>

</tasks>

<verification>
Open `css/style.css` and confirm:
1. `.stamina-bar` + `.stamina-bar__fill` with transition on width
2. `.combat-round-card` with border and padding
3. `.combat-summary` with centered layout
4. `.combat-log` + `.combat-log__entry` + `.combat-log__battle` for history
5. No syntax errors (CSS validates)
</verification>

<success_criteria>
- All 4 groups of CSS classes exist in style.css
- Stamina bar has transition animation on fill width
- Critical stamina state has pulsing animation
- Mobile responsive rule adjusts summary stats layout
</success_criteria>

<output>
After completion, create `.planning/phases/03-battle-system/03-02-SUMMARY.md`
</output>
