---
phase: 01-foundation
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - backend/models.py
  - backend/schemas.py
  - backend/database.py
  - backend/routers/sessions.py
  - backend/main.py
  - backend/routers/compat.py
autonomous: true
requirements: [INFRA-01]
must_haves:
  truths:
    - "PUT /api/sessions/{book} with a mechanics field persists and returns it"
    - "GET /api/sessions returns mechanics dict for each session"
    - "GET /api/state returns 404 (compat layer deleted)"
    - "Server starts without errors after compat.py deletion"
    - "Database recreates cleanly on startup"
  artifacts:
    - path: "backend/models.py"
      provides: "Session model with mechanics_json TEXT column"
      contains: "mechanics_json"
    - path: "backend/schemas.py"
      provides: "SessionCreate/Update/Response with mechanics dict field; no Legacy* classes"
      contains: "mechanics"
    - path: "backend/routers/sessions.py"
      provides: "Upsert reads/writes mechanics_json via json.dumps/loads"
      contains: "mechanics_json"
    - path: "backend/main.py"
      provides: "No compat router registration"
  key_links:
    - from: "backend/routers/sessions.py"
      to: "backend/models.py"
      via: "session.mechanics_json = json.dumps(body.mechanics)"
      pattern: "json\\.dumps\\(body\\.mechanics"
    - from: "backend/schemas.py"
      to: "backend/models.py"
      via: "model_validator decodes mechanics_json"
      pattern: "json\\.loads\\(data\\.mechanics_json"
---

<objective>
Extend the backend Session model with a `mechanics_json` TEXT column, update all Pydantic schemas to carry a `mechanics: dict` field, delete the legacy compat layer, and recreate the database.

Purpose: INFRA-01 requires book-specific mechanic state to survive save/load. The backend must persist and return an open-ended `mechanics` dict per session. The compat shim is dead code after storage.js migrates (Plan 03).
Output: Backend that accepts and returns `mechanics` on all session endpoints; compat.py deleted; DB recreated clean.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-foundation/01-CONTEXT.md
@.planning/phases/01-foundation/01-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Extend Session model, update Pydantic schemas, delete compat layer</name>
  <files>backend/models.py, backend/schemas.py, backend/main.py, backend/routers/compat.py</files>
  <read_first>backend/models.py, backend/schemas.py, backend/main.py, backend/routers/compat.py</read_first>
  <action>
1. **backend/models.py** -- Add `mechanics_json` column to Session model (per D-07):
   ```python
   mechanics_json = Column(Text, nullable=True, default='{}')
   ```
   Also add `name = Column(Text, nullable=True)` for character name (Claude's discretion -- Phase 2 needs it, adding now avoids a second DB recreate). Place both after `luck_current`.

2. **backend/schemas.py** -- Three changes (per D-05, D-08):
   a. Add `import json` at the top.
   b. Add `mechanics: dict = Field(default_factory=dict)` to `SessionCreate` (after `luck` field).
   c. Add `mechanics: dict | None = Field(default=None)` to `SessionUpdate` (after `luck` field). Use `None` to match the optional pattern of skill/stamina/luck in SessionUpdate.
   d. Add `mechanics: dict = Field(default_factory=dict)` to `SessionResponse` (after `luck` field).
   e. In `SessionResponse.assemble_stat_blocks`, add to the returned dict:
      ```python
      "mechanics": json.loads(data.mechanics_json or '{}'),
      ```
   f. Delete the three legacy classes: `LegacyStatBlock`, `LegacyGameEntry`, `LegacyStateBlob` (lines 52-65 currently).
   g. Do NOT delete `ActionRequest`, `ActionLogResponse`, or `ActionResult` -- those are used by actions.py.

3. **backend/main.py** -- Remove compat router (per D-04):
   a. Change line 9 from `from .routers import sessions, compat, actions` to `from .routers import sessions, actions`.
   b. Delete line 30: `app.include_router(compat.router, prefix="/api", tags=["compat"])`.

4. **backend/routers/compat.py** -- Delete the entire file (per D-04).
  </action>
  <verify>
    <automated>cd /home/claude/ffconsole && python -c "from backend.models import Session; from backend.schemas import SessionCreate, SessionResponse, SessionUpdate; assert hasattr(Session, 'mechanics_json'); assert 'mechanics' in SessionCreate.model_fields; assert 'mechanics' in SessionResponse.model_fields; print('OK')" && python -c "from backend.main import app; routes = [r.path for r in app.routes]; assert '/api/state' not in [r.path for r in app.routes if hasattr(r, 'path')]; print('No compat routes')" && test ! -f backend/routers/compat.py && echo "compat.py deleted"</automated>
  </verify>
  <acceptance_criteria>
    - backend/models.py contains `mechanics_json = Column(Text, nullable=True, default='{}')`
    - backend/models.py contains `name = Column(Text, nullable=True)`
    - backend/schemas.py contains `mechanics: dict = Field(default_factory=dict)` in SessionCreate
    - backend/schemas.py contains `mechanics: dict` in SessionResponse
    - backend/schemas.py contains `json.loads(data.mechanics_json or '{}')`
    - backend/schemas.py does NOT contain `LegacyStatBlock` or `LegacyGameEntry` or `LegacyStateBlob`
    - backend/main.py does NOT contain `compat`
    - backend/routers/compat.py does NOT exist
  </acceptance_criteria>
  <done>Session model has mechanics_json TEXT column. All three Pydantic schemas carry mechanics field. Legacy compat classes deleted. compat.py deleted. main.py no longer registers compat router.</done>
</task>

<task type="auto">
  <name>Task 2: Update sessions router to read/write mechanics_json, recreate DB</name>
  <files>backend/routers/sessions.py, backend/database.py</files>
  <read_first>backend/routers/sessions.py, backend/database.py, backend/models.py, backend/schemas.py</read_first>
  <action>
1. **backend/routers/sessions.py** -- Add `import json` at top. Update four places to handle mechanics_json:

   a. `create_session` (POST): When constructing the new Session object (around line 29-37), add:
      ```python
      mechanics_json=json.dumps(body.mechanics or {}),
      ```

   b. `upsert_session` (PUT): In the update branch (the `if session:` block, around line 55-62), add:
      ```python
      session.mechanics_json = json.dumps(body.mechanics or {})
      ```
      In the create branch (the `else:` block, around line 63-72), add `mechanics_json=json.dumps(body.mechanics or {}),` to the Session constructor.

   c. `patch_session` (PATCH): After the luck block (around line 90-92), add:
      ```python
      if body.mechanics is not None:
          session.mechanics_json = json.dumps(body.mechanics)
      ```

2. **backend/database.py** -- Update `init_db()` to drop and recreate (per D-01, D-02):
   ```python
   def init_db():
       Base.metadata.drop_all(bind=engine)
       Base.metadata.create_all(bind=engine)
   ```
   This ensures a clean schema with the new columns. The `drop_all` will be removed after Phase 1 (per Pitfall 2 awareness).

3. **Verify end-to-end**: After changes, start the server, PUT a session with mechanics, GET it back, confirm mechanics round-trips.
  </action>
  <verify>
    <automated>cd /home/claude/ffconsole && source venv/bin/activate && rm -f ff.db && python -c "
from backend.database import init_db; init_db()
from backend.database import SessionLocal
from backend.models import Session
import json
db = SessionLocal()
s = Session(book_number=99, skill_initial=10, skill_current=10, stamina_initial=20, stamina_current=20, luck_initial=8, luck_current=8, mechanics_json=json.dumps({'hero_points': 5}))
db.add(s); db.commit(); db.refresh(s)
from backend.schemas import SessionResponse
resp = SessionResponse.model_validate(s)
assert resp.mechanics == {'hero_points': 5}, f'Got {resp.mechanics}'
db.delete(s); db.commit(); db.close()
print('Round-trip OK')
"</automated>
  </verify>
  <acceptance_criteria>
    - backend/routers/sessions.py contains `import json`
    - backend/routers/sessions.py contains `json.dumps(body.mechanics` (at least 3 occurrences: create, upsert-update, upsert-create)
    - backend/routers/sessions.py contains `body.mechanics is not None` (in patch_session)
    - backend/database.py contains `Base.metadata.drop_all(bind=engine)`
    - A Session with mechanics_json='{"hero_points": 5}' round-trips through SessionResponse.model_validate and returns mechanics={'hero_points': 5}
  </acceptance_criteria>
  <done>Sessions router persists and returns mechanics on all CRUD operations. Database drops and recreates on startup ensuring clean schema with new columns. mechanics round-trips through the ORM and Pydantic layer.</done>
</task>

</tasks>

<verification>
1. `python -c "from backend.main import app; print('App loads')"` -- no import errors
2. Start server, `curl -X PUT localhost:3000/api/sessions/1 -H 'Content-Type: application/json' -d '{"book_number":1,"skill":{"initial":10,"current":10},"stamina":{"initial":20,"current":20},"luck":{"initial":8,"current":8},"mechanics":{"test":"value"}}'` returns 200 with `mechanics.test == "value"`
3. `curl localhost:3000/api/sessions` returns list with mechanics field
4. `curl localhost:3000/api/state` returns 404 or "not found"
</verification>

<success_criteria>
- Backend accepts and returns `mechanics` dict on PUT/POST/GET/PATCH for sessions
- Compat layer fully removed (no /api/state endpoint, no Legacy* schemas, no compat.py file)
- Database recreates with new schema on startup
- No import errors when starting the application
</success_criteria>

<output>
After completion, create `.planning/phases/01-foundation/01-01-SUMMARY.md`
</output>
