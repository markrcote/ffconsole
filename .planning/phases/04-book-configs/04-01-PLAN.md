---
phase: 04-book-configs
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - js/config/mechanics/default.js
  - js/config/mechanics/book-17.js
  - js/config/mechanics/book-30.js
  - js/config/mechanics/registry.js
  - css/style.css
autonomous: true
requirements: [AFEAR-01, AFEAR-02, AFEAR-03, CHAOS-01, CHAOS-02, CHAOS-03, CHAOS-04, CHAOS-05]

must_haves:
  truths:
    - "getBookConfig(17) resolves a config with extraStats, checklists, and superpower keys"
    - "getBookConfig(30) resolves a config with extraStats, resources, and checklists keys"
    - "getBookConfig(99) resolves the default config with empty arrays and no superpower key"
    - "CSS classes for checklist items and superpower display exist in style.css"
  artifacts:
    - path: "js/config/mechanics/book-17.js"
      provides: "Book 17 config with Hero Points, Clues checklist, superpower options"
      exports: ["config"]
    - path: "js/config/mechanics/book-30.js"
      provides: "Book 30 config with Kuddam, Tabasha, Provisions, Fuel, Spells, Special Abilities"
      exports: ["config"]
    - path: "js/config/mechanics/default.js"
      provides: "Base config with checklists: [] added"
      exports: ["config"]
    - path: "js/config/mechanics/registry.js"
      provides: "Registry with books 17 and 30 uncommented"
      exports: ["CONFIG_REGISTRY"]
    - path: "css/style.css"
      provides: "Checklist, superpower picker, superpower display CSS classes"
  key_links:
    - from: "js/config/mechanics/registry.js"
      to: "js/config/mechanics/book-17.js"
      via: "dynamic import thunk"
      pattern: "17:.*import.*book-17"
    - from: "js/config/mechanics/registry.js"
      to: "js/config/mechanics/book-30.js"
      via: "dynamic import thunk"
      pattern: "30:.*import.*book-30"
---

<objective>
Create the book-specific config data files for Books 17 and 30, extend the base config schema with checklists support, activate the registry entries, and add all CSS classes needed for the book mechanics UI.

Purpose: Establishes the data layer and visual styles that the renderer (Plan 02) and wiring (Plan 03) depend on.
Output: Four JS config files updated/created, CSS extended with checklist and superpower styles.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-book-configs/04-CONTEXT.md
@.planning/phases/04-book-configs/04-UI-SPEC.md

<interfaces>
<!-- Existing config schema from default.js -->
From js/config/mechanics/default.js:
```javascript
export const config = {
    bookNumber: null,
    extraStats: [],
    resources: [],
    combatVariant: 'standard',
    combatModifiers: {},
};
```

From js/config/mechanics/registry.js:
```javascript
export const CONFIG_REGISTRY = {
    // 17: () => import('./book-17.js'),
    // 30: () => import('./book-30.js'),
};
```

From js/books.js:
```javascript
async function getBookConfig(bookNumber) {
    const loader = CONFIG_REGISTRY[bookNumber];
    if (!loader) {
        const { config } = await import('./config/mechanics/default.js');
        return { ...config, bookNumber: bookNumber || null };
    }
    const { config } = await loader();
    return config;
}
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create book config files, extend default schema, activate registry</name>
  <read_first>
    - js/config/mechanics/default.js
    - js/config/mechanics/registry.js
    - .planning/phases/04-book-configs/04-CONTEXT.md
  </read_first>
  <files>
    js/config/mechanics/default.js,
    js/config/mechanics/book-17.js,
    js/config/mechanics/book-30.js,
    js/config/mechanics/registry.js
  </files>
  <action>
1. **default.js** — Add `checklists: []` to the config object, after `resources: []`. Update the schema JSDoc comment to include:
   `checklists: Array<{ id: string, label: string, items: Array<{ id: string, label: string }> }>`
   Also add `superpower: null` to the schema comment (but NOT to the default config object — only books with superpowers define it).

2. **book-17.js** — Create with `export const config = { ... }` containing:
   - `bookNumber: 17`
   - `extraStats: [{ id: 'hero_points', label: 'Hero Points', initial: 0, min: 0, max: null }]`
     (Hero Points in Appointment with F.E.A.R. start at 0 and are earned during the adventure; no upper cap)
   - `resources: []`
   - `checklists: [{ id: 'clues', label: 'Clues', items: [ ... 8 clue items ... ] }]`
     Use real clue names from the book. Research the actual clue names from Appointment with F.E.A.R. (Book 17). The 8 clues the player must collect are specific plot items/evidence. If exact names cannot be confirmed, use descriptive placeholders: "Clue 1" through "Clue 8" with IDs `clue-1` through `clue-8`.
   - `superpower: { options: ['Psi-Powers', 'Energy Blast', 'Flying', 'Super Strength', 'Laser Vision'] }`
   - `combatVariant: 'standard'`
   - `combatModifiers: {}`

3. **book-30.js** — Create with `export const config = { ... }` containing:
   - `bookNumber: 30`
   - `extraStats: [
       { id: 'kuddam_defeats', label: 'Kuddam Defeated', initial: 0, min: 0, max: null },
       { id: 'tabasha', label: 'Tabasha', initial: 0, min: 0, max: 4 }
     ]`
   - `resources: [
       { id: 'provisions', label: 'Provisions', initial: 10, min: 0, max: null, step: 1 },
       { id: 'fuel', label: 'Fuel', initial: 10, min: 0, max: null, step: 1 }
     ]`
     (Provisions and Fuel start at sensible defaults; exact initial values are book-dependent — 10 is a reasonable default)
   - `checklists: [
       { id: 'spells', label: 'Spells', items: [ ... spell items ... ] },
       { id: 'special_abilities', label: 'Special Abilities', items: [ ... ability items ... ] }
     ]`
     Research the actual spell names and special ability names from Chasms of Malice (Book 30). If exact names cannot be confirmed, use numbered placeholders.
   - `combatVariant: 'standard'`
   - `combatModifiers: {}`

4. **registry.js** — Uncomment both import thunks per D-03:
   ```javascript
   export const CONFIG_REGISTRY = {
       17: () => import('./book-17.js'),
       30: () => import('./book-30.js'),
   };
   ```
  </action>
  <verify>
    <automated>cd /home/claude/ffconsole && node -e "
      import('./js/config/mechanics/default.js').then(m => {
        const c = m.config;
        console.assert(Array.isArray(c.checklists), 'default has checklists array');
        console.assert(c.checklists.length === 0, 'default checklists empty');
        console.log('default.js OK');
      });
      import('./js/config/mechanics/book-17.js').then(m => {
        const c = m.config;
        console.assert(c.bookNumber === 17, 'book 17 number');
        console.assert(c.extraStats.length === 1, 'book 17 has 1 extraStat');
        console.assert(c.extraStats[0].id === 'hero_points', 'hero_points id');
        console.assert(c.superpower.options.length === 5, 'book 17 has 5 superpower options');
        console.assert(c.checklists.length === 1, 'book 17 has 1 checklist');
        console.assert(c.checklists[0].id === 'clues', 'clues checklist');
        console.log('book-17.js OK');
      });
      import('./js/config/mechanics/book-30.js').then(m => {
        const c = m.config;
        console.assert(c.bookNumber === 30, 'book 30 number');
        console.assert(c.extraStats.length === 2, 'book 30 has 2 extraStats');
        console.assert(c.resources.length === 2, 'book 30 has 2 resources');
        console.assert(c.checklists.length === 2, 'book 30 has 2 checklists');
        console.log('book-30.js OK');
      });
      import('./js/config/mechanics/registry.js').then(m => {
        console.assert(typeof m.CONFIG_REGISTRY[17] === 'function', 'registry has 17');
        console.assert(typeof m.CONFIG_REGISTRY[30] === 'function', 'registry has 30');
        console.log('registry.js OK');
      });
    "</automated>
  </verify>
  <acceptance_criteria>
    - default.js config object contains `checklists: []` key
    - book-17.js exports config with bookNumber 17, extraStats containing hero_points, superpower with 5 options, checklists with clues group
    - book-30.js exports config with bookNumber 30, extraStats containing kuddam_defeats and tabasha, resources containing provisions and fuel, checklists containing spells and special_abilities
    - registry.js has both entries uncommented: `17: () => import('./book-17.js')` and `30: () => import('./book-30.js')`
    - All files use ES module `export const config` pattern
  </acceptance_criteria>
  <done>All four config files created/updated. getBookConfig(17) and getBookConfig(30) resolve their respective configs via dynamic import.</done>
</task>

<task type="auto">
  <name>Task 2: Add CSS classes for checklist items, superpower picker, and superpower display</name>
  <read_first>
    - css/style.css
    - .planning/phases/04-book-configs/04-UI-SPEC.md
  </read_first>
  <files>css/style.css</files>
  <action>
Append the following CSS rules AFTER the existing Phase 2 dice roller section at the end of style.css. Add a section comment `/* -- Phase 4: Book Mechanics ------------------------------------------------ */`.

Add these classes exactly as specified in UI-SPEC:

1. `.book-mechanics-section` — No additional styles beyond inherited `.mechanics-section`. Just a semantic hook.

2. `.checklist-group` — `margin-top: 16px`

3. `.checklist-items` — `list-style: none; padding: 0; margin: 0`

4. `.checklist-item` — `display: flex; align-items: center; gap: 8px; padding: 8px 0; border-bottom: 1px dashed var(--paper-shadow)`

5. `.checklist-item:last-child` — `border-bottom: none`

6. `.checklist-item input[type="checkbox"]` — `width: 20px; height: 20px; accent-color: var(--ink-color); cursor: pointer; flex-shrink: 0`

7. `.checklist-item label` — `font-family: 'Caveat', cursive; font-size: 1.1rem; color: var(--ink-color); cursor: pointer; line-height: 1.4`

8. `.checklist-item.checked label` — `text-decoration: line-through; color: var(--ink-light)`

9. `.superpower-options` — `display: flex; flex-direction: column; gap: 8px`

10. `.superpower-option` — `width: 100%; text-align: left; padding-left: 16px` (extends `.mechanic-btn` via combined class in HTML)

11. `.superpower-display` — `margin-top: 8px`

12. `.superpower-display__value` — `font-family: 'Caveat', cursive; font-size: 1.1rem; color: var(--ink-color); margin: 4px 0 0 0`

13. `.book-mechanics-subtitle` — No new class needed; reuses `.mechanics-title` in HTML.

No responsive overrides needed — checklist items are already full-width, and `.mechanic-btn` already has `width: 100%` at mobile breakpoint.
  </action>
  <verify>
    <automated>cd /home/claude/ffconsole && grep -c "checklist-group" css/style.css && grep -c "checklist-item" css/style.css && grep -c "superpower-options" css/style.css && grep -c "superpower-display__value" css/style.css</automated>
  </verify>
  <acceptance_criteria>
    - css/style.css contains `.checklist-group` with `margin-top: 16px`
    - css/style.css contains `.checklist-item` with `display: flex` and `gap: 8px`
    - css/style.css contains `.checklist-item.checked label` with `text-decoration: line-through`
    - css/style.css contains `.checklist-item input[type="checkbox"]` with `accent-color: var(--ink-color)`
    - css/style.css contains `.superpower-options` with `flex-direction: column`
    - css/style.css contains `.superpower-option` with `width: 100%`
    - css/style.css contains `.superpower-display__value` with `font-family: 'Caveat'`
    - All new CSS is appended after existing content, not inserted into existing rule blocks
    - Section comment `Phase 4: Book Mechanics` present
  </acceptance_criteria>
  <done>All CSS classes from UI-SPEC exist in style.css. Checklist items, superpower picker buttons, and superpower read-only display are visually styled.</done>
</task>

</tasks>

<verification>
- `node -e "import('./js/config/mechanics/registry.js').then(r => console.log(Object.keys(r.CONFIG_REGISTRY)))"` prints `['17', '30']`
- `grep 'checklist-item' css/style.css` returns multiple matches
- `grep 'superpower' css/style.css` returns multiple matches
</verification>

<success_criteria>
- Book 17 and Book 30 config files exist with correct schema-compliant data
- Default config includes checklists array
- Registry activates both books via dynamic import
- CSS contains all checklist and superpower visual classes per UI-SPEC
</success_criteria>

<output>
After completion, create `.planning/phases/04-book-configs/04-01-SUMMARY.md`
</output>
